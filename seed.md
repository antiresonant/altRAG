Plan mode. I want to make an efficient way for you to reference skills so that you use precise information with minimal context bloat. We're creating an alternative to vector DBs and RAG but for skills. The retrieval should utilise pointers. So for all indexed skill files available to you on any machine, we're gonna utilise a table of pointers that directly refers to the memory location of specific information within the skills file. For example, in well-formatted skill file, different aspect of the skills are directly mentioned in MD headings with hashtags. Creating a tree of pointers that's structured hierarchially with the level of the heading allows for far better skill access and also allows an architectural scaffolding for the agent to plan when doing a task, with far less context bloat. For this we can simply have a scan.c file that gets pointers to these specific references. scan.c should also be primarily dynamic. The base version should always recognize levels of headings to build the tree but in case of some ill formatted files, the llm itself should run one-pass through it and either modify the .c file to retrieve the information (if it's in another standard format like xml or yaml), or bring the file itself to a standard form before running the scan.c. The tree should then be stored in something simple like a .tsv or .csv or something that's even more appropriate for pointers: the main goal here being the format that allows the LLM to interpret the tree in the easiest way. Then when the agent is running in plan mode, it views the skill architecture when planning so that when it executes, bloat is so minimal that accuracy of task completion is on a surgical level.
